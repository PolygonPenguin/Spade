// Shape.spade
// Defines the Shape interface.  Interfaces specify a contract that
// implementing classes must fulfill.

interface Shape uses Default: // 'uses Default' for Float and String.

// All shapes must provide a way to calculate their area.
public Float getArea();

// All shapes must provide a string representation.
public String toString();